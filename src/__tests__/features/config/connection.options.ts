import { ConnectionOptions } from '../../../connection';

export const connectionOptions: ConnectionOptions = {
	driver: 'postgres',
	user: 'cokeorm',
	password: 'cokeorm',
	database: 'cokeorm',
	host: 'localhost',
	entities: [],
	triggers: [],
	subscribers: [],
	logger: false,
};
